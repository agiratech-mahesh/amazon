pipeline { 
    agent any 
        stages { 
            stage ('checkout') { 
                steps { 
                     checkout scm
                }  
            }
            stage ('build') {
            tools {
             maven maven 'maven 3.8.5'
             }
            steps { 
            sh 'maven clean build'
                
                 }
                 }
        }
    }
