pipeline { 
    agent any 
        stages { 
            stage ('checkout') { 
                steps { 
                     checkout scm
                }  
            }
            stage ('build') {
            tools {
             maven 'maven 3.6'
             }
            steps { 
            sh 'mvn clean build'
                
                 }
                 }
        }
    }
